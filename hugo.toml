baseURL = 'https://wikiforge.github.io/'
languageCode = 'en-us'
title = 'WikiForge Help'

[module]
    [[module.imports]]
        path = "github.com/colinwilson/lotusdocs"
        disable = false
    [[module.imports]]
        path = "github.com/gohugoio/hugo-mod-bootstrap-scss/v5"
        disable = false

[params.social]
        github = "WikiForge"
        twitter = "WikiForge"

[params.docs]
        # pathName      = "docs"
        darkMode        = true
        sidebarIcons    = true
        titleIcon       = true
        lastMod         = true